{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "docs" }
    }
  ],
  "routes": [
    { "src": "/assets/(.*)", "dest": "/docs/assets/$1" },
    { "src": "/(.*)",          "dest": "/docs/index.html" }
  ]
}